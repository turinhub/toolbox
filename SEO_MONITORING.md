# SEO Monitoring

Turinhub Toolbox targets Chinese search first. After each SEO-facing release,
use this checklist to keep indexing and search traffic measurable.

## Domain Configuration

The official toolbox origin is `https://toolbox.turinhub.com`. `https://turinhub.com` is the company website.

Set `NEXT_PUBLIC_SITE_URL` to the deployment's public origin before building. An absent or blank value falls back to `https://toolbox.turinhub.com` in [lib/seo.ts](./lib/seo.ts). Metadata, JSON-LD, sitemap entries, and the sitemap URL generated by [app/robots.ts](./app/robots.ts) share this configuration. Changing the origin requires a rebuild and deployment.

## Deployment verification pending

The review of commit `dc010b8` on 2026-09-06 found that the production homepage canonical and sitemap entries used `https://turinhub.com`, despite the toolbox being served at `https://toolbox.turinhub.com`. The current source corrects the fallback and replaces the static robots file with a generated route. These source changes do not update an existing deployment.

Before releasing, verify the production `NEXT_PUBLIC_SITE_URL` value is the toolbox origin or unset. After deploying, verify canonical, language alternates, JSON-LD, Open Graph URLs, sitemap entries, and the robots sitemap URL against the live site. Remove this pending-verification notice only after those checks pass; do not submit sitemap contents that still use the company domain.

## Launch Checks

- Before submitting, verify that canonical, Open Graph URLs, JSON-LD, sitemap entries, and `robots.txt` use the toolbox origin, not the company website or localhost.
- Submit `https://toolbox.turinhub.com/sitemap.xml` in Google Search Console.
- Submit `https://toolbox.turinhub.com/sitemap.xml` in Baidu Search Resource Platform.
- Verify that `/`, `/tools`, `/en`, `/en/tools`, and representative tool pages in both languages are indexable and return 200.
- Verify that language alternates point to the corresponding Chinese and English routes.
- Check that `/api/` stays blocked in `robots.txt`.
- Run Rich Results Test or Schema Markup Validator for `/`, `/tools`, and at
  least five tool pages.

## Weekly Metrics

- Natural search landing pages.
- Search impressions, clicks, CTR, and average position.
- Indexed pages and excluded pages.
- Top Chinese queries for each tool category.
- Core Web Vitals for `/`, `/tools`, JSON Formatter, Prompt Optimizer, and GPU
  Calculator.

## Follow-up Rules

- If a tool page receives impressions but low CTR, revise its title and
  description in `lib/routes.ts`.
- If a page is crawled but not indexed, add clearer use cases and internal links.
- If a page has poor mobile Web Vitals, fix the rendering or client bundle before
  adding more content.
